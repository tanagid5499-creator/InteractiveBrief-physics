# Assets

This static app uses code-native canvas diagrams for the main interactive scale-reading demonstrations.

The readable extracted vernier caliper image is copied here from `../reference/extracted-assets/image1.png` so the static app remains self-contained when served from `interactive-lab/`. WMF/EMF assets from the Word document are not used in the browser UI.
